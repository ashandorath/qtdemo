interface:

clicking on a line will insert a point on that line.
You can click on a circle and drag it (like moving a file)
clicking somewhere in the drawing field will attach a new point to the currently selected polygon. The point will be between the last/first and second last point.
The user is responsible for polygons being non-complex.

Minimum and maximum values can be set with the corresponding fields and clicking apply. 
